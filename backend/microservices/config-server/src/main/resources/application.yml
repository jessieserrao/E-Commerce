spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations #place where the configuration will be fetched (repository)
server:
  port: 8888
